import './bootstrap';

import {
    computePosition,
    flip,
    shift,
    offset,
    autoUpdate,
} from '@floating-ui/dom';

window.DanumFloatingUI = {
    computePosition,
    flip,
    shift,
    offset,
    autoUpdate,
};